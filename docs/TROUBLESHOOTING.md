# Troubleshooting

If a repair workflow behaves unexpectedly:
- confirm the current device evidence rather than reusing old assumptions
- inspect the current tool permissions and active agent profile
- verify whether the workflow is in trusted-local mode
- check whether the device is physically reported, host-detected, or only charging

If the host reports no USB data path, do not treat charging as interface availability.
