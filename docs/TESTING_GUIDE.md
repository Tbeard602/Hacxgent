# Testing Guide

Verified commands:
- `uv run pytest -q`
- `uv run pytest -q tests/test_samsung_replay_acceptance.py`
- `uv build`

Current status:
- repository test suite passes
- Samsung replay acceptance passes repeatedly
- package build and clean-install smoke test pass

Static checks remain a separate backlog item and are not all green yet.
